{% for seccion in secciones %}
Seccion: {{ seccion }}
  {% for e in secciones[ seccion ] %}
  Elemento: {{ e }}
    {% for i in secciones[ seccion ][ e ] %}
    {{ i }}: {{  secciones[ seccion ][ e ][ i ] }}
    {% endfor %}
  {% endfor %}
{% endfor %}

